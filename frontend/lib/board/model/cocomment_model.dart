class CoCommentModel {
  final String userid, postId, commentId, cocomentId, name, content, likeCount;

  CoCommentModel(
    this.userid,
    this.postId,
    this.commentId,
    this.cocomentId,
    this.name,
    this.content,
    this.likeCount,
  );
}
