class CommentModel {
  final String userid, postId, commentId, name, content, likeCount;

  CommentModel(
    this.userid,
    this.postId,
    this.commentId,
    this.name,
    this.content,
    this.likeCount,
  );
}
